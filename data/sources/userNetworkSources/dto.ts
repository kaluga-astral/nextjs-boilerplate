export namespace UserNetworkSourcesDTO {
  export type ContactDTO = {
    email?: string;
    phone: string;
  };

  export type PersonDTO = {
    name: string;
    surname: string;
    displayName: string;
  };
}
