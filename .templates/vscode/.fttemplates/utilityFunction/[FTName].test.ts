import { describe, it } from 'vitest';

import { [FTName] } from './[FTName]';

describe('[FTName]', () => {
  describe('', () => {
    it('', () => {
      const result = [FTName]();

      expect(result).toBe();
    });
  });
});
