import { describe, it } from '@example/shared/_tests';

import { createCacheService } from '@example/shared';

import { <FTName | capitalize> } from './<FTName | capitalize>';

describe('<FTName | capitalize>', () => {
  it('', () => {
      const sut = new <FTName | capitalize>(createCacheService());
  });
});
