---
to: <%=h.dataRepositoriesPath(`${h.inflection.camelize(name, false)}Repository/dto.ts`) %>
---
import { describe, it } from 'vitest'

import {`${h.inflection.camelize(name, false)}Repository`} from './`${h.inflection.camelize(name, false)}Repository'


describe('', () => {
   describe('', () => {
        it('', () => {

        });
   })
});

